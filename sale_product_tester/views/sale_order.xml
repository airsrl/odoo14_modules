<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="sale_order_form_inherit" model="ir.ui.view">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <field name="add_tester" invisible="1"/>
                    <button name="get_tester" type="object" string="Inserisci tester" attrs="{'invisible':['|',('add_tester','=',False),('state','not in',['draft'])]}"/>
                </xpath>
                <xpath expr="//field[@name='order_line']/tree" position="inside">
                    <field name="allowed_tester" invisible="1"/>
                    <field name="has_tester" invisible="1"/>
                    <field name="is_tester" invisible="1"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>